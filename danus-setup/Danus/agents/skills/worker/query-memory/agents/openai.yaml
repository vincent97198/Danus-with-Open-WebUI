interface:
  display_name: "Query Memory"
  short_description: "Search prior local reasoning artifacts"
  default_prompt: "Use $query-memory to retrieve previously saved immediate conclusions, toy examples, counterexamples, failed paths, or branch states from memory."

policy:
  allow_implicit_invocation: true
